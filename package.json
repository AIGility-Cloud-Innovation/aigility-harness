{
  "name": "aigility-harness",
  "version": "0.1.0",
  "private": true,
  "description": "Five-layer pluggable AI Agent architecture with DSH-Cordis kernel adapter",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "example:prototype": "pnpm --filter prototype-mode start"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.23.12",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {}
}
